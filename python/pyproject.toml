[project]
name = "mcp-server-starter"
version = "1.0.0"
description = "A minimal, working MCP server over stdio."
requires-python = ">=3.11"
dependencies = [
  # Pinned on purpose: the server API differs between the 1.x FastMCP line and
  # mcp 2.x (mcp.server.mcpserver.MCPServer).
  "mcp==2.2.0",
]
